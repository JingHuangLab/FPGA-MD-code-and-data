#
Some programs for MD simulations and generate coeffcient tables.

In the `tools` folder.

Run 
```bash
make + `program_name`
```

where `program_name` should be one of:
 - board 
 - create_coef_table
 - md_sim
 - mdlj
 - create_particle_file
 - gen_inter_table 

